---
import {Image} from "astro:assets";
import Layout from "@/layouts/SlideLayout.astro";
import shopSearch from "@/assets/shopSearch.png";
---

<Layout next="/allgemeines/static-sites" prev="/toc" slideId="1">
    <h1>Was macht Astro?</h1>

    <div class="flex mt-8 gap-4 items-center justify-center">
        <div class="w-1/3">
            <Image
              src={shopSearch}
              loading="lazy"
              width={500}
              height={500}
              alt="Shop Suche"
              class="w-68"
            />
        </div>
        <div>
            <ul>
                <li class="!text-xl" data-step>
                    <strong>Server-side rendering:</strong> Der Server generiert HTML für jede Anfrage.
                </li>
                <li class="!text-xl" data-step>
                    <strong>Client-side rendering:</strong> Der Browser lädt JavaScript, das die Seite dynamisch
                    generiert.
                </li>
                <li class="!text-xl" data-step>
                    <strong>Static site generation:</strong> Der Server generiert HTML-Dateien in Build-Zeit, die dann
                    direkt ausgeliefert werden.
                </li>
            </ul>
        </div>
    </div>
</Layout>
